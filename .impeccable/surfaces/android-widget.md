# 홈 화면 달력 위젯

Scope: Android AppWidget and its native configuration activity. Operate mode. Extend the existing note-series identity; the user explicitly requested a compact month grid with readable, single-line event titles. Code-led native geometry; no concept seed is applicable to this specified layout.

## Direction contract

THESIS: Use every date cell for real schedule titles. Height determines line capacity, and an overflow count preserves awareness when space runs out.

OWN-WORLD: Inherit the dark note-series surface, muted separators, restrained lavender today marker, and actual calendar colors. Offer deliberate light, system and transparent variants for wallpapers.

STORY: Add from the app or launcher, select calendars, check the live preview, save. Each widget owns its preferences and month navigation.

FIRST VIEWPORT: A narrow month navigation strip, seven aligned weekday labels, four to six equal week rows, and a compact freshness indicator. Date numbers are subordinate to the readable event lines; long titles ellipsize without wrapping.

FORM: User-confirmed full-screen monthly form, default 4×5 launcher cells with two-axis resizing. This is a specified native layout within the existing series, not a new visual-world selection.

SIGNATURE INTERACTION: Resizing reveals more event lines without changing the month structure. Tap a day to continue in the app; navigate months without opening it. Settings show the same renderer used by the launcher.

QUALITY BAR: Native configuration controls, system insets and Back cancellation; explicit stale/empty/out-of-range states; accessible date descriptions, correct recurring and multi-day events, independent instances and durable private snapshots. App changes update the widget immediately; server refresh uses the app's existing authenticated connection.

FINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance
